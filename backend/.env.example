# Render preenche DATABASE_URL automaticamente pelo render.yaml.
# Para rodar localmente, aponte para um PostgreSQL local ou para uma
# conexão externa de um banco PostgreSQL de desenvolvimento.
DATABASE_URL=postgresql://postgres:senha@localhost:5432/ecobuild
SESSION_SECRET=troque-por-uma-chave-segura
NODE_ENV=development
PORT=3000
